<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_onboarding"
    android:padding="24dp">

    <ImageView
        android:id="@+id/ivAssetIcon1"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:contentDescription="Asset Icon"
        android:src="@drawable/logo"
        android:elevation="8dp"
        app:layout_constraintBottom_toTopOf="@id/cardTextContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <LinearLayout
        android:id="@+id/cardTextContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:layout_marginHorizontal="8dp"
        android:background="@drawable/bg_card_onboarding"
        android:orientation="vertical"
        android:padding="28dp"
        android:elevation="4dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ivAssetIcon1">

        <TextView
            android:id="@+id/tvTitleAsset1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="Manajemen Inventaris Aset"
            android:textColor="@color/accent"
            android:textSize="24sp"
            android:fontFamily="sans-serif-black"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvDescAsset1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="Kemudahan dalam mencatat, mendata kondisi fisik, serta melacak lokasi seluruh barang dan aset instansi Anda secara real-time."
            android:textColor="#555555"
            android:textSize="15sp"
            android:lineSpacingMultiplier="1.2"
            android:fontFamily="sans-serif-medium" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>